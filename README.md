# vince-portfolio

Single-page personal portfolio for Vince Macaraig — AI & Automation Engineer (marketing & growth data).

- **Stack:** plain static HTML/CSS/JS. No build step.
- **Deploy:** Vercel — Framework preset `Other`, no build command, root directory `./`.
- **Edit copy:** all content is in `index.html`. Styling tokens at the top of `styles.css`.

## TODO before sharing the link widely
- Replace `LINKEDIN-URL-TBD` in `index.html` with the real LinkedIn URL.
- Add a dedicated contact email button (NOT a Walker Advertising address).
- Add 2–3 automation demo links/Looms under "What I build" once recorded.
